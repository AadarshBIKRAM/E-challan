import pymysql
from flask import render_template

# DB CONN
# conn = pymysql.connections.Connection(host='localhost', user='root', passwd='', database='e-challan')
# cursor = conn.cursor()


# Function to add data

# def search():
#     return render_template("checkResult.html")
# def adding(txt_val):
#     cursor.execute('INSERT INTO valadding(id,value) VALUES(DEFAULT,%s)', txt_val)
#     conn.commit()
#     return 1
    # Closing the cursor and connection to the database


# def retriving():
#     cursor.execute('SELECT * FROM valadding')
#     rows = cursor.fetchall()
#     return rows
